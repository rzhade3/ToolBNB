html 
	include components/header.pug
	body
		include components/navbar.pug
		.container-fluid.outDiv
			.innerdiv
				h1
					span#one Toolbnb
					p Book unique tools <br> and experience a tool like an owner.
	form#searchForm(action="/borrow", method="POST", class="search-wrapper")
		input(id="searchbar" list="autocomplete" name="searchvalue" type="text" placeholder="Search" required="")
		datalist(id="autocomplete")
			option(value="Drills")
			option(value="Saws")
			option(value="Screwdrivers")
			option(value="Wrenches")
		

